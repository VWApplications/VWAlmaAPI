version: '3'
# Homolog enviroment
services:
  accounts-homolog:
    image: victorhad/accounts:homolog
    container_name: accounts-homolog
    build:
      context: .
      dockerfile: ./enviroments/homolog/Dockerfile
    ports:
      - "8000:8080"