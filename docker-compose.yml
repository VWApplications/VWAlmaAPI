version: '3'
# Development enviroment
services:
  accounts-local:
    image: victorhad/accounts:local
    container_name: accounts-local
    build:
      context: .
      dockerfile: ./enviroments/local/Dockerfile
    volumes:
      - .:/home/developer/
    ports:
      - "8000:8080"